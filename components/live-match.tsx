"use client";
import { useEffect,useState } from "react"; import { ArrowLeft, ArrowLeftRight, ChevronRight, MessageCircle, Pause, Play, SlidersHorizontal, Sparkles, Zap } from "lucide-react"; import type { ClubState,Player } from "@/lib/domain";
export default function LiveMatch(){const [state,setState]=useState<ClubState|null>(null);const [view,setView]=useState("Live");const [drawer,setDrawer]=useState<"sub"|"tactics"|"talk"|"shout"|null>(null);async function act(action:string,extra:Record<string,unknown>={}){const r=await fetch("/api/state",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,...extra})});const b=await r.json();setState(b.state);if(b.error)alert(b.error)}useEffect(()=>{fetch("/api/state").then(r=>r.json()).then(b=>setState(b.state))},[]);if(!state)return <div className="grid min-h-screen place-items-center bg-[var(--bg)] text-sm text-[var(--muted)]">Loading match room…</div>;const m=state.match;const starters=state.tactics.starters.map(id=>state.players.find(p=>p.id===id)).filter(Boolean) as Player[];const bench=state.tactics.bench.map(id=>state.players.find(p=>p.id===id)).filter(Boolean) as Player[];return <div className="min-h-screen bg-[#07090c] px-3 pb-8 pt-4 text-[var(--text)] sm:px-6"><header className="mx-auto flex max-w-3xl items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-lg bg-[var(--amber)] text-xs font-black text-[#15130d]">RA</span><div><p className="eyebrow">Live match · Northport Ground</p><p className="mt-1 text-xs font-black">{m.phase} · {m.minute}'</p></div></div><span className="tag border-[var(--red)] text-[var(--red)]">LIVE ROOM</span></header><main className="mx-auto mt-4 max-w-3xl"><section className="panel overflow-hidden"><div className="grid grid-cols-[1fr_auto_1fr] items-center border-b border-[var(--line)] p-5 text-center"><div><div className="mx-auto grid h-11 w-11 place-items-center rounded-xl bg-[var(--amber)] font-black text-[#15130d]">RA</div><p className="mt-2 text-xs">Riverside</p></div><div><p className="text-5xl font-black tracking-[-.08em]">{m.homeGoals}<span className="mx-2 text-[var(--muted)]">:</span>{m.awayGoals}</p><p className="mono mt-2 text-[10px] text-[var(--muted)]">{m.lastAction}</p></div><div><div className="mx-auto grid h-11 w-11 place-items-center rounded-xl bg-[var(--panel2)] font-black text-[var(--muted)]">CV</div><p className="mt-2 text-xs">Crown Vale</p></div></div><div className="grid grid-cols-4 border-b border-[var(--line)]">{["Live","Commentary","Stats","Ratings"].map(x=><button key={x} onClick={()=>setView(x)} className={`py-3 text-[10px] font-black ${view===x?"bg-[var(--amber)] text-[#15130d]":"text-[var(--muted)]"}`}>{x}</button>)}</div>{view==="Live"&&<div className="p-4"><div className="grid grid-cols-3 gap-2"><Stat l="Possession" v={`${m.possession}%`}/><Stat l="Opponent plan" v={m.opponentPlan}/><Stat l="Mentality" v={state.tactics.mentality}/></div><Momentum values={m.momentum}/><div className="mt-3 flex items-center justify-between"><p className="eyebrow">Momentum · Riverside advantage</p><span className="mono text-xs text-[var(--amber)]">{Math.round(m.momentum.at(-1)??50)} / 100</span></div></div>}{view==="Commentary"&&<div className="max-h-[46vh] space-y-3 overflow-y-auto p-4">{m.events.length?m.events.map(e=><div key={e.id} className="flex gap-3 border-l-2 border-[var(--amber)] pl-3"><span className="mono text-xs text-[var(--amber)]">{e.minute}'</span><div><p className="text-sm font-black">{e.headline}</p><p className="mt-1 text-xs leading-5 text-[var(--muted)]">{e.body}</p></div></div>):<p className="text-sm text-[var(--muted)]">Kick-off is ready. The assistant will surface tactical moments as they happen.</p>}</div>}{view==="Stats"&&<div className="grid grid-cols-2 gap-2 p-4">{[["Possession",`${m.possession}%`,`50%`],["Shots",`${m.events.filter(e=>e.type==="Chance"||e.type==="Goal").length}`,"2"],["On target","1","0"],["Corners","2","1"],["Fouls","3","5"],["Passes","118","97"]].map(([l,h,a])=><div key={l} className="rounded-xl bg-[var(--panel2)] p-3"><p className="eyebrow">{l}</p><div className="mt-2 flex justify-between text-sm font-black"><span>{h}</span><span className="text-[var(--muted)]">{a}</span></div></div>)}</div>}{view==="Ratings"&&<div className="space-y-2 p-4">{starters.map(p=><div key={p.id} className="flex items-center gap-3"><span className="tag w-8 text-center">{p.position}</span><p className="flex-1 text-xs font-black">{p.name}</p><div className="h-1.5 w-24 rounded-full bg-[var(--panel2)]"><div className="h-full rounded-full bg-[var(--green)]" style={{width:`${p.form*10}%`}}/></div><span className="mono text-xs">{p.form.toFixed(1)}</span></div>)}</div>}<div className="grid grid-cols-2 gap-2 border-t border-[var(--line)] p-4"><button disabled={m.phase==="Full time"} onClick={()=>void act("tick")} className="action flex items-center justify-center gap-2 py-3 text-xs"><Play className="h-4 w-4"/>{m.paused?"Paused":"Continue 5 minutes"}</button><button onClick={()=>void act("match-action",{id:"pause"})} className="ghost flex items-center justify-center gap-2 py-3 text-xs"><Pause className="h-4 w-4"/>{m.paused?"Resume":"Pause"}</button></div></section><section className="mt-3 grid grid-cols-4 gap-2"><Action icon={<SlidersHorizontal/>} label="Tactics" onClick={()=>setDrawer("tactics")}/><Action icon={<ArrowLeftRight/>} label="Sub" onClick={()=>setDrawer("sub")}/><Action icon={<Zap/>} label="Shout" onClick={()=>setDrawer("shout")}/><Action icon={<MessageCircle/>} label="Talk" onClick={()=>setDrawer("talk")}/></section>{drawer==="sub"&&<Drawer title="Make a substitution" close={()=>setDrawer(null)}><p className="eyebrow">Choose player to replace</p><div className="mt-3 space-y-2">{starters.map(out=><div key={out.id} className="rounded-xl border border-[var(--line)] p-3"><div className="flex items-center gap-2"><span className="tag">{out.position}</span><p className="flex-1 text-xs font-black">{out.name}</p><span className="mono text-xs">{out.fitness}%</span></div><div className="mt-2 flex gap-2">{bench.map(inc=><button key={inc.id} onClick={async()=>{await act("substitute",{id:out.id,inId:inc.id});setDrawer(null)}} className="ghost flex-1 py-2 text-[10px]">{inc.name} on</button>)}</div></div>)}</div></Drawer>}{drawer==="tactics"&&<Drawer title="Touchline tactics" close={()=>setDrawer(null)}><p className="text-xs leading-5 text-[var(--muted)]">Changes apply immediately to the next match passage.</p><div className="mt-3 grid grid-cols-2 gap-2">{[["Positive","Raise mentality"],["Attacking","Commit numbers"],["Wide","Stretch them"],["All over","Press now"]].map(([v,l])=><button key={v} onClick={async()=>{await act("patch",{patch:{tactics:{...state.tactics,mentality:v,shape:v==="Wide"?"Wide":state.tactics.shape,closingDown:v==="All over"?"All over":state.tactics.closingDown}}});setDrawer(null)}} className="ghost p-3 text-left"><p className="text-xs font-black">{v}</p><p className="mt-1 text-[10px] text-[var(--muted)]">{l}</p></button>)}</div></Drawer>}{drawer==="shout"&&<Drawer title="Touchline shout" close={()=>setDrawer(null)}><div className="space-y-2">{["Demand more intensity","Hold the shape","Attack the wide spaces","Slow the game"].map(x=><button key={x} onClick={async()=>{await act("match-action",{id:"shout"});setDrawer(null)}} className="ghost flex w-full items-center justify-between p-3 text-left text-xs font-black">{x}<ChevronRight className="h-4 w-4 text-[var(--muted)]"/></button>)}</div></Drawer>}{drawer==="talk"&&<Drawer title="Team talk" close={()=>setDrawer(null)}><p className="eyebrow">Immediate mood intervention</p><div className="mt-3 grid grid-cols-3 gap-2">{["Demanding","Balanced","Relaxed"].map(x=><button key={x} onClick={async()=>{await act("match-action",{id:"team-talk"});setDrawer(null)}} className="ghost py-3 text-[10px] font-black">{x}</button>)}</div></Drawer>}</main></div>}
function Stat({l,v}:{l:string;v:string}){return <div className="rounded-xl bg-[var(--panel2)] p-3"><p className="eyebrow">{l}</p><p className="mt-2 truncate text-sm font-black">{v}</p></div>}
function Action({icon,label,onClick}:{icon:React.ReactNode;label:string;onClick:()=>void}){return <button onClick={onClick} className="ghost flex flex-col items-center gap-1 p-3 text-[10px] font-black">{icon}{label}</button>}
function Momentum({values}:{values:number[]}){return <div className="mt-5 flex h-24 items-end gap-1 rounded-xl border border-[var(--line)] bg-[#0c1319] p-3">{values.map((v,i)=><div key={i} className={`flex-1 rounded-t ${v>=50?"bg-[var(--amber)]":"bg-[var(--blue)]"}`} style={{height:`${Math.max(12,Math.min(100,v))}%`}}/> )}</div>}
function Drawer({title,close,children}:{title:string;close:()=>void;children:React.ReactNode}){return <div className="fixed inset-x-0 bottom-0 z-30 mx-auto max-w-3xl rounded-t-3xl border border-[var(--line)] bg-[#11161c] p-5 shadow-2xl"><div className="mx-auto mb-4 h-1 w-10 rounded-full bg-[var(--line)]"/><div className="flex items-center justify-between"><h2 className="text-xl font-black">{title}</h2><button onClick={close} className="text-2xl text-[var(--muted)]">×</button></div><div className="mt-4 max-h-[60vh] overflow-y-auto">{children}</div></div>}
