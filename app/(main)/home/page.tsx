"use client";

import { Header } from "@/components/navigation/Header";
import { Badge } from "@/components/ui/Badge";
import { Button } from "@/components/ui/Button";
import { Card } from "@/components/ui/Card";
import { EmptyState } from "@/components/ui/EmptyState";
import { useClubState } from "@/hooks";
import { competitionService, fixtureService, trainingService } from "@/services";
import { Activity, CalendarDays, ChevronRight, Clock3, RefreshCcw, ShieldCheck, Sparkles, WalletCards } from "lucide-react";
import { motion } from "framer-motion";
import { useRouter } from "next/navigation";

const initials = (value: string) => value.split(" ").map((word) => word[0]).join("").slice(0, 3).toUpperCase();

export default function HomePage() {
  const router = useRouter();
  const { club, isLoading, error, refresh } = useClubState();
  const nextFixture = fixtureService.getNextFixture();
  const table = competitionService.getLeagueTable().slice(0, 5);
  const training = trainingService.getTrainingOverview();
  if (isLoading) return <div><Header title="HOME" subtitle="Loading club state" /><div className="mx-auto grid max-w-[1400px] gap-4 px-4 py-5 sm:px-6 lg:grid-cols-12 lg:px-8"><div className="shimmer h-64 rounded-2xl lg:col-span-8" /><div className="shimmer h-64 rounded-2xl lg:col-span-4" /><div className="grid gap-4 sm:grid-cols-3 lg:col-span-12">{[1, 2, 3].map((item) => <div key={item} className="shimmer h-32 rounded-2xl" />)}</div></div></div>;
  if (error || !club) return <div><Header title="HOME" subtitle="Club state unavailable" /><EmptyState icon={<RefreshCcw className="h-7 w-7" />} title="Dashboard unavailable" description={error ?? "No club state has been created yet."} action={<button onClick={() => void refresh()} className="rounded-xl bg-accent-lime px-4 py-2 text-xs font-bold text-surface-0">Try again</button>} /></div>;
  return <div><Header title="HOME" subtitle={`${club.name} · ${club.league}`} /><div className="mx-auto grid max-w-[1400px] grid-cols-1 gap-4 px-4 py-5 sm:px-6 lg:grid-cols-12 lg:gap-5 lg:px-8">{nextFixture && <motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} className="lg:col-span-8"><Card className="relative min-h-[260px] overflow-hidden p-5 sm:p-7"><div className="absolute inset-0 bg-[radial-gradient(circle_at_80%_20%,rgba(182,109,255,.18),transparent_35%),linear-gradient(120deg,rgba(183,245,42,.08),transparent_50%)]" /><div className="relative flex h-full flex-col justify-between"><div><p className="eyebrow">Next match</p><div className="mt-8 flex items-center justify-between gap-3"><div><div className="flex h-14 w-14 items-center justify-center rounded-2xl border border-surface-400 bg-surface-100 text-sm font-bold text-accent-lime">{initials(club.name)}</div><p className="mt-2 text-sm font-semibold text-surface-950">{club.name}</p></div><div className="text-center"><p className="text-xs text-surface-600">{nextFixture.date} · {nextFixture.time}</p><p className="mt-2 text-2xl font-bold text-surface-950">VS</p><p className="mt-1 text-xs text-surface-600">{nextFixture.isHome ? "Home fixture" : "Travel fixture"}</p></div><div className="text-right"><div className="ml-auto flex h-14 w-14 items-center justify-center rounded-2xl border border-surface-400 bg-surface-100 text-sm font-bold text-surface-800">{initials(nextFixture.opponent)}</div><p className="mt-2 text-sm font-semibold text-surface-950">{nextFixture.opponent}</p></div></div></div><div className="mt-8 flex flex-wrap items-center gap-3"><Button onClick={() => router.push("/match/prep")}><Sparkles className="mr-2 h-4 w-4" />Prepare match</Button><span className="inline-flex items-center gap-2 text-xs text-surface-600"><CalendarDays className="h-4 w-4" />{nextFixture.isHome ? "Home venue" : "Travel fixture"}</span></div></div></Card></motion.div>}<motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .08 }} className="lg:col-span-4"><Card className="h-full p-5"><div className="flex items-center justify-between"><div><p className="eyebrow">Today&apos;s schedule</p><p className="mt-1 text-xs text-surface-600">{training.weekLabel}</p></div><button onClick={() => router.push("/more/calendar")} className="text-xs font-semibold text-accent-lime">See all</button></div><div className="mt-5 space-y-2">{training.weeklyPlan.slice(0, 4).map((item, index) => <button key={`${item.date}-${item.type}`} onClick={() => router.push("/more/training")} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left transition hover:border-surface-500 ${index === 0 ? "border-accent-lime/50 bg-accent-lime/5" : "border-surface-400 bg-surface-100"}`}><Clock3 className="h-4 w-4 shrink-0 text-accent-lime" /><span className="min-w-0 flex-1"><span className="block truncate text-xs font-semibold text-surface-950">{item.type}</span><span className="mt-1 block truncate text-[11px] text-surface-600">{item.sub}</span></span><span className="text-[11px] text-surface-600">{item.date}</span></button>)}</div></Card></motion.div><div className="grid gap-4 sm:grid-cols-3 lg:col-span-12"><Card className="p-5"><div className="flex items-center justify-between"><span className="text-xs uppercase tracking-wider text-surface-600">Board confidence</span><ShieldCheck className="h-4 w-4 text-accent-lime" /></div><p className="mt-4 text-3xl font-bold text-accent-lime">{club.boardConfidence}</p><p className="mt-1 text-xs text-surface-600">Derived from current morale and board state</p></Card><Card className="p-5"><div className="flex items-center justify-between"><span className="text-xs uppercase tracking-wider text-surface-600">Squad morale</span><Activity className="h-4 w-4 text-accent-lime" /></div><p className="mt-4 text-3xl font-bold text-accent-lime">{club.squadMorale}</p><p className="mt-1 text-xs text-surface-600">Calculated from player morale and fitness</p></Card><Card className="p-5"><div className="flex items-center justify-between"><span className="text-xs uppercase tracking-wider text-surface-600">Financial health</span><WalletCards className="h-4 w-4 text-accent-purple" /></div><p className="mt-4 text-3xl font-bold text-surface-950">£{(club.finances.balance / 1_000_000).toFixed(1)}M</p><p className="mt-1 text-xs text-surface-600">Transfer budget £{(club.finances.transferBudget / 1_000_000).toFixed(1)}M</p></Card></div><motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="lg:col-span-7"><Card className="h-full p-5"><div className="mb-4 flex items-center justify-between"><div><p className="eyebrow">League table</p><p className="mt-1 text-xs text-surface-600">Current competition snapshot</p></div><Badge variant="lime">Top 5</Badge></div><div className="space-y-1">{table.map((row) => <div key={row.club} className={`flex items-center justify-between rounded-xl px-3 py-3 ${row.club === club.name ? "border border-accent-lime/40 bg-accent-lime/5" : "border border-transparent"}`}><div className="flex items-center gap-3"><span className="w-4 text-xs text-surface-600">{row.position}</span><span className={`text-sm font-semibold ${row.club === club.name ? "text-accent-lime" : "text-surface-950"}`}>{row.club}</span></div><div className="flex items-center gap-5 text-xs text-surface-600"><span>{row.played}</span><span>{row.gd > 0 ? "+" : ""}{row.gd}</span><strong className="text-surface-950">{row.points}</strong></div></div>)}</div><button onClick={() => router.push("/more/competitions")} className="mt-4 flex w-full items-center justify-between border-t border-surface-400 pt-4 text-sm font-semibold text-surface-800">Full table <ChevronRight className="h-4 w-4 text-accent-lime" /></button></Card></motion.div><div className="grid gap-4 lg:col-span-5"><Card className="p-5"><div className="flex items-center justify-between"><p className="eyebrow">Recent form</p><span className="text-xs text-surface-600">Last 5</span></div><div className="mt-5 flex gap-2">{club.recentForm.map((result, index) => <div key={`${result.opponent}-${index}`} className={`flex h-11 flex-1 items-center justify-center rounded-xl text-sm font-bold ${result.result === "W" ? "bg-accent-lime/20 text-accent-lime" : result.result === "D" ? "bg-surface-300 text-surface-800" : "bg-accent-red/20 text-accent-red"}`}>{result.result}</div>)}</div></Card><Card className="p-5"><div className="flex items-center justify-between"><p className="eyebrow">Latest news</p><button onClick={() => router.push("/more/inbox")} className="text-xs font-semibold text-accent-lime">All news</button></div><div className="mt-4 space-y-3">{club.news.length ? club.news.slice(0, 2).map((item) => <button key={item.id} onClick={() => router.push("/more/inbox")} className="flex w-full items-center gap-3 text-left"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-surface-300 text-xs font-bold text-accent-lime">{item.title.slice(0, 1)}</div><span className="min-w-0 flex-1"><span className="block truncate text-xs font-semibold text-surface-950">{item.title}</span><span className="mt-1 block text-[11px] text-surface-600">{item.timeAgo}</span></span><ChevronRight className="h-4 w-4 text-surface-600" /></button>) : <p className="text-sm text-surface-600">No club news is available.</p>}</div></Card></div></div></div>;
}
